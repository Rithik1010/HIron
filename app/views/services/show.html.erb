<% provide(:title, @service.serv_name) %>
  <div class="row">
    <h1><%= @service.serv_name %></h1> 
    <% if @service.partners.any? %>
      <h3>Service Partners (<%= @service.partners.count %>)</h3>
      <ol class="partners">
        <% @service.partners.each do |partner| %>
          <li>
            <a class="partner removelinkdefault"> <%= link_to partner.partner_name, partner %> &nbsp</a>  
            <a class="avg-review-rating" data-score=<%= partner.reviews.average(:rating) %>></a>
          </li>
        <% end %>
      </ol>
    <% end %>
  </div>

<script>
  $('.avg-review-rating').raty();
  $('.avg-review-rating').raty({
    readOnly: true,
    score: function() {
      return $(this).attr('data-score');
    },
    path: '/assets/'
  });
</script>